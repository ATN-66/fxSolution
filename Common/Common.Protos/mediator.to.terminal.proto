syntax = "proto3";
package google.protobuf;
import "grpcquotation.proto";

service MediatorToTerminal {
    rpc TickAsync(stream google.protobuf.grpcquotation) returns (stream Reply);
}

message Reply {
    string replyMessage = 1;
}