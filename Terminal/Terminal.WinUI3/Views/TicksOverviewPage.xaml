<Page
    x:Class="Terminal.WinUI3.Views.TicksOverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Terminal.WinUI3.Behaviors"
    xmlns:converters="using:Terminal.WinUI3.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:maintenance="using:Terminal.WinUI3.Models.Maintenance"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <behaviors:NavigationViewHeaderBehavior.HeaderMode>Never</behaviors:NavigationViewHeaderBehavior.HeaderMode>

    <Page.Resources>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter" />

        <DataTemplate x:Key="DailyTemplate" x:DataType="maintenance:DailyContribution">
            <Rectangle
                Width="30"
                Height="30"
                Margin="3"
                Fill="{x:Bind HasContribution, Converter={StaticResource BoolToColorConverter}}" />
        </DataTemplate>

        <DataTemplate x:Key="MonthlyTemplate" x:DataType="maintenance:MonthlyContribution">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Text="{x:Bind Month}" FontSize="16" FontWeight="Bold" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ItemsRepeater ItemsSource="{x:Bind DailyContributions}">
                        <ItemsRepeater.ItemTemplate>
                            <StaticResource ResourceKey="DailyTemplate" />
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </StackPanel>

            </Grid>
        </DataTemplate>

    </Page.Resources>
    <Grid>
        <ItemsRepeaterScrollHost>
            <ScrollViewer x:Name="scrollViewer">
                <ItemsRepeater x:Name="yearRepeater" ItemsSource="{x:Bind ViewModel.Groups}">
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="maintenance:YearlyContribution">
                            <StackPanel Orientation="Vertical">
                                <TextBlock FontSize="20" FontWeight="Bold" Text="{x:Bind Year}" />
                                <ItemsRepeater ItemsSource="{x:Bind Months}">
                                    <ItemsRepeater.ItemTemplate>
                                        <StaticResource ResourceKey="MonthlyTemplate" />
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </ScrollViewer>
        </ItemsRepeaterScrollHost>
    </Grid>
</Page>
